version: '2'
services:
  photon:
    build:
      context: .
      dockerfile: Dockerfile
    image: jfelectron/geocoder
    volumes:
      - data:/photon/photon_data
    ports:
      - 2322:2322

volumes:
  data:
    driver: local
